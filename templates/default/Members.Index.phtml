<h1>
	<small><?= $community_name ?></small>
	<?php __("members.title") ?>
</h1>

<div class="pull-clear push-10">
	<ul class="pagination condensed pull-right">
		<?= $letter_first ?>
		<?= $letter_list ?>
		<?= $numbers ?>
	</ul>
</div>

<div class="block padding-no">
	<table class="table">
		<thead>
			<tr>
				<th colspan="6">
					<?php if($term): ?>
						<?php __("members.search") ?>: "<?= $term ?>"
					<?php endif; ?>
					<div class="pull-right text-s0">
						<a href="members?<?= $letter_param ?>order=name" class="label invert"><?php __("members.member_name") ?></a>
						<a href="members?<?= $letter_param ?>order=join" class="label invert"><?php __("members.join_date") ?></a>
						<a href="members?<?= $letter_param ?>order=post" class="label invert"><?php __("members.post_count") ?></a>
					</div>
				</th>
			</tr>
			<tr>
				<td class="min"></td>
				<td><?php __("members.member_name") ?></td>
				<td><?php __("members.joined") ?></td>
				<td><?php __("members.posts") ?></td>
				<td><?php __("members.location") ?></td>
				<td><?php __("members.group") ?></td>
			</tr>
		</thead>
		<tbody>
			<?php if(!empty($results)): ?>
				<?php foreach($results as $k => $v): ?>
					<tr>
						<td class="min"><?= \AC\Kernel\Html::crop($results[$k]['avatar'], 40) ?></td>
						<td class="font-w600">
							<a href="profile/<?= $results[$k]['m_id'] ?>"><?= $results[$k]['username'] ?></a>
						</td>
						<td><?= $results[$k]['joined'] ?></td>
						<td><?= $results[$k]['posts'] ?></td>
						<td><?= $results[$k]['location'] ?></td>
						<td><?= $results[$k]['name'] ?></td>
					</tr>
				<?php endforeach; ?>
			<?php else: ?>
				<tr>
					<td colspan="6" class="text-center"><?php __("members.no_results") ?></td>
				</tr>
			<?php endif; ?>
		</tbody>
	</table>
</div>

<div class="block">
	<form method="get" class="inline">
		<p class="form-control-static"><?php __("members.search") ?>:</p>
		<input type="text" name="term" minlength="3" class="form-control span-3" required>
		<input type="submit" class="btn btn-default" value="OK">
	</form>
</div>
